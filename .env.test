# .env file for testing
# نسخة خاصة للاختبارات

NODE_ENV=test
PORT=3001

# Test Database (استخدم DB منفصلة للاختبارات)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=nfc_test_db

# JWT (استخدم قيم تجريبية)
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRE=1h

# Disable Redis في الاختبارات
# REDIS_URL=

SITE_BASE_URL=http://localhost:3001
ADMIN_TOKEN=test-admin-token
